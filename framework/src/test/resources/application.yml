spring.application.name: test-app

cqrs.event-handling:
  standard:
    retry:
      policy: fixed
      initial-interval: 5

otel:
  traces:
    exporter: none
  metrics:
    exporter: none
  logs:
    exporter: none