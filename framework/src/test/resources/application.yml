spring.application.name: test-app

cqrs.event-handling:
  standard:
    retry:
      policy: fixed
      initial-interval: 5

management:
  opentelemetry:
    tracing:
      export:
        enabled: false
  otlp:
    metrics:
      export:
        enabled: false